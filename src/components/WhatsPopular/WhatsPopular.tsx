import SectionTitle from '@/utils/SectionTitle';
import React from 'react';

const WhatsPopular = () => {
  return (
    <div>
      <SectionTitle section="What's Popular" color='#000' switches={['On TV', 'In Theaters']} />
    </div>
  );
};

export default WhatsPopular;
